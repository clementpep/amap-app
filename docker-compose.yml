services:
  amap-web:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        # Set these in Dokploy > Service > Build Args (not Env Vars)
        SUPABASE_URL: ${SUPABASE_URL}
        SUPABASE_ANON_KEY: ${SUPABASE_ANON_KEY}
    image: amap-web:latest
    restart: unless-stopped
    # Routing is handled by Dokploy's built-in Traefik — configure domain
    # and container port (80) in Dokploy UI > Domains tab.
    # No manual labels or port bindings needed.
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost/index.html"]
      interval: 30s
      timeout: 5s
      retries: 3
